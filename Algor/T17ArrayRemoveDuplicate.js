var removeDuplicates = function (nums) {
  for (let j = 0; j < nums.length; j++) {
    console.log(nums[j]);
    let lowest = nums[j];
    for (let k = j; k < nums.length; j++) {
      if (lowest == nums[k]) {
      }
    }
  }
};

removeDuplicates([1, 1, 2]);
