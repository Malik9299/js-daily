namespace SMS {
  class Student {
    private rolleNumber: number;
    private name: string;
    private address: string;

    constructor(rolleNumber: number, name: string, address: string) {
      this.rolleNumber = rolleNumber;
      this.name = name;
      this.address = address;
    }
  }
}
